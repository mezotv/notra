{
  "name": "turbo",
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo run build",
    "dev": "dotenv -- turbo run dev",
    "check-types": "dotenv -- turbo run check-types",
    "db:generate": "dotenv -- pnpm exec drizzle-kit generate --config packages/db/drizzle.config.ts",
    "db:migrate": "dotenv -- pnpm exec drizzle-kit migrate --config packages/db/drizzle.config.ts",
    "db:push": "dotenv -- pnpm exec drizzle-kit push --config packages/db/drizzle.config.ts",
    "db:studio": "dotenv -- pnpm exec drizzle-kit studio --config packages/db/drizzle.config.ts",
    "db:drop": "dotenv -- pnpm exec drizzle-kit drop --config packages/db/drizzle.config.ts",
    "db:pull": "dotenv -- pnpm exec drizzle-kit pull --config packages/db/drizzle.config.ts",
    "email:dev": "dotenv -- pnpm --filter @notra/email dev",
    "check": "ultracite check",
    "format": "ultracite fix",
    "format:unsafe": "ultracite fix --unsafe"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.2",
    "dotenv-cli": "^11.0.0",
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "openlogs": "^0.0.1",
    "turbo": "^2.9.5",
    "ultracite": "7.1.4"
  },
  "engines": {
    "node": "24.11.1"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "pnpm@10.33.0"
}
