---
title: "Granola"
description: "Connect Granola with an API key so the Notra agent can read meeting notes, AI summaries, and transcripts"
authors:
  - handle: dominik

docolin:
  schema_version: 1
  kind: tools/notra/integrations/granola
  type: how-to
  applies_to:
    - notra
    - granola
  language: en
  difficulty: beginner
  time_estimate: 3m
  status: stable
  aliases:
    - Granola integration
    - meeting notes
    - Granola API key
  prev: ./slack.mdx
  next: ./google-search-console.mdx
---

The Granola integration gives the Notra agent read access to your [Granola](https://www.granola.ai/) meeting notes. Once connected, you can ask the agent in **Chat** or in [Slack](/integrations/slack) to pull context from recent meetings, summarize a call, or draft content from a transcript.

## What the agent can do

The agent gets three tools per connected Granola workspace:

* **List notes**: meeting notes with their AI summaries, attendees, and timestamps, filtered by date range or folder.
* **Get a note**: a single note with its summary and metadata, optionally including the full transcript.
* **List folders**: your Granola folders, so it can scope a search.

Access is read-only. Notra never creates, edits, or deletes anything in Granola.

## Setup

<Steps>
  <Step title="Create a Granola API key">
    In the Granola app, open **Settings**, then **Connectors**, then **API keys**, and create a key. Keys start with `grn_`. Granola's own guide is at [docs.granola.ai](https://docs.granola.ai/help-center/sharing/integrations/granola-api).
  </Step>

  <Step title="Add the integration in Notra">
    In the dashboard sidebar, open **Integrations** and click **Connect** on the **Granola** card, or open the Granola page and click **Connect Granola**. Enter a **Display name** (for example "Team meeting notes") and paste the **API key**.
  </Step>

  <Step title="Save">
    Notra verifies the key against Granola before saving it. If the key is rejected you get an error and nothing is stored. On success the key is encrypted and the toast reads "Granola connected".
  </Step>
</Steps>

## Managing the integration

The Granola page lists every connected workspace. From the card menu you can enable or disable it (a disabled integration is hidden from the agent) or delete it. Deleting removes the encrypted key from Notra; revoke the key in Granola as well if you no longer need it.

## Limits

- Granola is available to the agent only. It is not a source for the **Create Content** dialog, schedules, or event triggers.
- The agent reads notes on request. Notra does not sync or store your Granola notes.
